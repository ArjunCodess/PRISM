# PRISM environment
# Copy values into the host that needs them. Do not commit real .env files.

# Website (apps/web). Required for a public deploy. Baked in at Next build time.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# API (apps/api). No secrets are required for the research exhibit.
# PYTHONPATH=ml/src
# PORT=8000
